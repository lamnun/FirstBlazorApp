@page "/counter"

<h1>Counter 여기다 제목</h1>

<p>Current count(오른쪽에 카운터변수들어감): @currentCount</p>

@* 버튼 연결, 이름 설정 *@  @*IncrementCount <- 이거 쓰면 걍 카운트*@
<button class="btn btn-primary" @onclick="IncrementCount">Click me???</button>


@code {
    private int currentCount = 0; /*카운트 초기값*/

    private void IncrementCount()
    {
        currentCount += 2;
    }
}


@*
        @page "/counter"
    <h1>Counter</h1>
    <p>The counter value is @counter</p>
    @code {
    private int counter = 42;
*@
